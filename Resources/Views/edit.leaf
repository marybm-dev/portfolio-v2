#extend("private")

#export("body") {
<div class="container">
    <div class="row">
        <br><br>
        <div class="u-full-width">
            <h4>Edit Project</h4>
        </div>
    </div>
    
    <form action="/projects" method="post">
        <div class="row">
            <div class="eight columns">
                <label>Title</label>
                <input name="title" class="u-full-width" type="text" value="#(project.title)"/>
            </div>
            <div class="four columns">
                <label>Type</label>
                <select class="u-full-width" name="type" value="#(project.type)">
                    <option value="Mobile">Mobile</option>
                    <option value="Web">Web</option>
                </select>
            </div>
        </div>
        
        <div class="row">
            <label>Description</label>
            <textarea class="u-full-width" name="description" class="u-full-width">#(project.description)</textarea>
        </div>
        
        <div class="row">
            <label>Image</label>
            <input name="image" class="u-full-width" type="text" value="#(project.image)"/>
        </div>
        
        <div class="row">
            <label>Video</label>
            <input name="video" class="u-full-width" type="text" value="#(project.video)"/>
        </div>
        
        <div class="row">
            <label>Link</label>
            <input name="image" class="u-full-width" type="text" value="#(project.link)"/>
        </div>
        
        <div class="row">
            <input class="button-primary" type="submit"value="Save">
        </div>
    </form>
</div>
}
